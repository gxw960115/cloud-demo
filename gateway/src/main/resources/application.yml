server:
  port: 10010
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: localhost:8848
#      ephemeral: true
#        cluster-name: HZ
#        namespace: 7094422b-94ff-418f-810b-3f99e9beb3cf
    gateway:
      routes:
        - id: user-service
          uri: lb://userservice
          predicates:
            - Path=/user/**